name: Test Runner Execution

on:
  workflow_dispatch:

jobs:
  test:
    name: Simple Test
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Echo test
        run: echo "Runner is working!"
