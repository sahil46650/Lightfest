name: Deploy to Production (Simple Test)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Application
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test step
        run: echo "Simple workflow test"
